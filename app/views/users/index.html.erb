<h1 class="text-2xl font-bold">
  The list of users
</h1>

<ul class="my-8 p-2 rounded-md border w-fit min-w-80">
  <% @users.each do |user| %>
    <li class="py-0.5 hover:font-bold">
      <%= link_to "👤 " + user.username, user, class: "block" %>
    </li>
  <% end %>
</ul>

<%= 
  button_to "Add new user", new_user_path, method: :get,
  class: "bg-green-600 text-white px-3 py-1.5 rounded-md cursor-pointer"
%>